<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug variant network güvenlik politikası.

  Production (main) variant cleartext'i tamamen kapatır. Debug build'lerde
  geliştirici makinasındaki backend (localhost) ve Android emulator'ün host
  loopback adresi (10.0.2.2) için cleartext gereklidir; ngrok / cloudflared
  tünelleri zaten HTTPS olduğu için burada yer almaz.

  Sadece bu host listesi cleartext-permitted işaretlenir; başka bir HTTP
  endpoint debug build'de bile reddedilir — typo'lu base URL erken sinyal
  verir.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
